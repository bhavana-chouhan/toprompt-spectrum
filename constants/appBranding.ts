/**
 * Project-wide branding constants — auto-emitted by generate_component_library.
 * Imported by app/_layout.tsx's <AppSplash> render and by any screen that
 * needs to display the app name without prop-drilling.
 */

export const APP_NAME = 'Spectrum' as const;
export const APP_TAGLINE: string | undefined = undefined;
export const REFERENCE_PRESET_ID: string | undefined = undefined;
